# CryEngine Code Scaffold

`RustSimulationComponent.*` is intended to be copied or referenced from a CryEngine C++ project/plugin.

It expects the shared header at:

```text
../../rust-engine/include/rust_engine.h
```

It expects the native library to be staged by:

```bash
../../scripts/build_native_plugin.sh
```

